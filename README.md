# MERN_Stack_G08
This is repository for g08.


1..10 | ForEach-Object { $folder = "day_{0:D2}" -f $_; mkdir $folder; "## $folder" | Out-File "$folder/README.md" }

--> git add . --> this will stages your changes

--> git commit -m"Day 2 content is added" --> this will save changes

--> git push origin main
